import styled from 'styled-components/native';

const Notifications = () => {
    return(
        <Container>
            
        </Container>
    );
};

export default Notifications;

const Container = styled.View``;